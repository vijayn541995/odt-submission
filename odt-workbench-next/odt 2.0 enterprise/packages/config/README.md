# Shared Config Package

Shared configuration for browser, native, services, and tests.

Target role:

- app constants
- route IDs
- feature flags
- provider-safe config
- upload limits
- local development ports
- environment variable names

Do not store secrets here.
